import React from 'react';

const SectionTitle = ({title}) => {
    return (
        <div>
            <h1 className='text-5xl font-bold text-[#253D4E] text-center my-6'>{title}</h1>
        </div>
    );
};

export default SectionTitle;