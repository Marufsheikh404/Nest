import React from 'react';

const SectionTitle = ({title}) => {
    return (
        <div>
            <h1 className='text-5xl font-semibold text-[#253D4E]'>{title}</h1>
        </div>
    );
};

export default SectionTitle;